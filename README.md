# WII Tennis game kata

##### This is a gradle java (Java 8+ required) project to solve WII tennis game kata:
---
`gradlew clean build` : to build project
`gradlew test`: to execute tests
`gradlew run`: to run main class.

